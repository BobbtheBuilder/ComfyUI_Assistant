chatbot_config.json
chatbot_history.json
kb_cache/
# local databases (kb + memory + generated stores)
*.sqlite
*.sqlite-*
debug.log
debug.log*
tests/report.json
__pycache__/
*.pyc

# graft's local graph cache — regenerable, not committed (run `graft build`).
/graft/

# local agent / editor tooling, not part of the node
AGENTS.md
opencode.json
.ignore

# comfy node publish build artifact
node.zip
